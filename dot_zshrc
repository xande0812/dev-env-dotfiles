# managed by chezmoi (dev-env-dotfiles). 編集は source 側 (dot_zshrc) で。

# --- PATH ---
# ~/.local/bin: pip --user / pipx 等が入れる user-local binary を PATH に出す。
# mise activate より前に置くことで mise の pinned shim を優先しつつ system より先にする。
# typeset -U path で再 source 時の重複を排除する (path は PATH に連動する zsh 配列)。
typeset -U path
path=("$HOME/.local/bin" $path)

# mise: 開発ツール (node/ripgrep/eza/starship/sheldon/git-wt/ghq 等) を PATH に出す。
# 他の eval より先に置く必要がある。
eval "$(mise activate zsh)"

# --- history ---
HISTFILE="$HOME/.zsh_history"
HISTSIZE=100000
SAVEHIST=100000
# fish ライクに「ターミナルごとに独立した履歴」にする。
# - SHARE_HISTORY は使わない: 他の開いているセッションのコマンドを即時取り込まないので、
#   各端末/tmux ペインの ↑履歴がそのセッション固有になる。
# - INC_APPEND_HISTORY: 実行ごとに即 HISTFILE へ追記する。clobber(複数端末の上書き)を防ぎ、
#   かつ新しく開いた端末は過去の全コマンドを読める(= 共有ファイルに蓄積はされる)。
setopt INC_APPEND_HISTORY HIST_IGNORE_DUPS HIST_IGNORE_ALL_DUPS HIST_EXPIRE_DUPS_FIRST

# --- completion + plugins ---
# sheldon: zsh-completions(fpath) → compinit → fzf-tab の順で source する。
eval "$(sheldon source)"

# git-wt: `git wt <branch>` 実行後に新 worktree へ自動 cd する git() wrapper。
# compinit / fzf-tab の後に評価する必要がある。
eval "$(git-wt --init zsh)"

# ghq + fzf: `ghq list` を fzf で選んで cd する widget。Ctrl-G にバインド。
ghq-fzf-widget() {
  local selected
  selected=$(ghq list | fzf) || return
  if [ -n "${selected}" ]; then
    BUFFER="cd $(ghq root)/${selected}"
    zle accept-line
  fi
  zle reset-prompt
}
zle -N ghq-fzf-widget
bindkey '^g' ghq-fzf-widget

# zsh-autosuggestions: widget を wrap するので fzf-tab の後・syntax-highlighting の前。
[ -r /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh ] && \
  source /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh

# --- history search (fish ライクな前方一致の ↑/↓) ---
# 入力中の文字列を prefix として一致する履歴だけを ↑/↓ でたどる。
# (空入力なら通常の履歴移動。複数行 buffer 内では行移動を優先する組込み widget。)
autoload -U up-line-or-beginning-search down-line-or-beginning-search
zle -N up-line-or-beginning-search
zle -N down-line-or-beginning-search
bindkey '^[[A' up-line-or-beginning-search    # Up
bindkey '^[[B' down-line-or-beginning-search  # Down
bindkey '^[OA' up-line-or-beginning-search    # Up   (application/keypad mode)
bindkey '^[OB' down-line-or-beginning-search  # Down (application/keypad mode)

# --- prompt ---
eval "$(starship init zsh)"

# direnv: cd した瞬間に .envrc を読み、プロジェクトごとの env へ切り替える。
# direnv は mise 管理 (mise activate で PATH に出る)。未許可の .envrc は実行されない
# (whitelist は direnv.toml に入れていない = 各プロジェクトで明示 allow する opt-in)。
eval "$(direnv hook zsh)"

# --- aliases (eza ベース) ---
alias ls='eza'
alias ll='eza -l --git'
alias la='ls -al'
alias lt='eza --tree'
alias lla='eza -la --git'
alias lg='lazygit'

# AI agent CLI は既定で bwrap sandbox 版を呼ぶ (低権限 = ANTHROPIC_API_KEY / OAuth のみ)。
# commit signing / AWS read-only / dotenv 復号が要るときは claude-with-* / codex-with-*
# を明示的に呼ぶ。生バイナリは `command claude` / `mise which claude` で。
# wrapper 本体は ansible が /usr/local/bin に配置する (dev-env-playbook/roles/bwrap_wrappers)。
alias claude='claude-sandboxed'
alias codex='codex-sandboxed'
alias cl='claude-sandboxed'
alias cl-g='claude-with-git'
alias cl-aws='claude-with-aws-ro'
alias cl-env='claude-with-dotenv'
alias cx='codex-sandboxed'
alias cx-g='codex-with-git'
alias cx-aws='codex-with-aws-ro'
alias cx-env='codex-with-dotenv'

# --- functions ---

# ntmux [name]: 今のシェルの env を引き継いだ、専用 socket (`-L`) の tmux server を
# <name> という名前で起動 / そこへ attach する。AWS_* や dotenv 等の secret 入りシェルを
# socket 単位で隔離した tmux に閉じ込める用途。
#   - name 省略時は ntmux-<PID> (呼び出しシェルの PID) を socket 名にして新規 server を
#     立てる。detach 後は `ntmux-attach` (socket が唯一なら自動再接続 / 複数なら一覧表示)
#     か `ntmux-ls` で socket を確認して入り直せる (PID を ps で辿る必要はない)。
#   - `-L $name` で毎回別 socket になるので、env (credential) がこの server だけに
#     焼かれる (他 session に漏れない)。同名なら既存 server に attach するだけ。
#   - `-e` で env を渡さない = secret が ps / history に乗らない。env は exec した
#     tmux server プロセスが現シェルから継承する。
ntmux() {
  emulate -L zsh
  local name=${1:-ntmux-$$}

  # 既にその専用 server があれば入るだけ
  if tmux -L "$name" has-session 2>/dev/null; then
    exec tmux -L "$name" attach
  fi
  # 無ければ: 今のシェル (env 入り) から新規 server を起動
  exec tmux -L "$name" new-session -s main
}

# ntmux-ls: 自分の ntmux-* 専用 socket と、その中の session を一覧する。
# 素の tmux は default socket (= `tmux ls` に出る側) なので、そこには現れない
# ntmux 側の生存中 server を確認する用。
ntmux-ls() {
  emulate -L zsh
  local sockdir=${TMUX_TMPDIR:-/tmp}/tmux-$(id -u)
  local socks=("$sockdir"/ntmux-*(N))
  if (( ${#socks} == 0 )); then
    print -r -- "ntmux socket なし ($sockdir)"
    return 1
  fi
  local s
  for s in $socks; do
    print -r -- "=== ${s:t} ==="
    tmux -L "${s:t}" ls 2>/dev/null
  done
}

# ntmux-attach [socket]: ntmux 専用 server へ再接続する (PID を ps で辿らずに復帰する用)。
#   - socket 指定時はそこへ attach。
#   - 省略時: ntmux-* socket が 1 個ならそこへ自動接続、複数なら一覧して指定を促す。
# detach しても server 自体は生き続けるので、何度でもこれで入り直せる。
# ntmux 本体と違い exec しない = detach すると呼び出し元シェルへ戻る (再接続だけなので
# 元シェルの env は server に影響しない)。
ntmux-attach() {
  emulate -L zsh
  local sockdir=${TMUX_TMPDIR:-/tmp}/tmux-$(id -u)
  if [[ -n "$1" ]]; then
    tmux -L "$1" attach
    return
  fi
  local socks=("$sockdir"/ntmux-*(N))
  case ${#socks} in
    0) print -r -- "ntmux socket なし ($sockdir)"; return 1 ;;
    1) tmux -L "${socks[1]:t}" attach ;;
    *) print -r -- "ntmux socket が複数あります。ntmux-attach <socket> で指定してください:"
       print -rl -- ${socks:t}; return 1 ;;
  esac
}

# --- egress: squid の明示 proxy(3128) に固定 ---
# 透過 intercept(3130) は S3 / CloudFront 等のラウンドロビン DNS で squid の
# host-forgery 検知に当たり、HTTP 409 で切られて TLS が intermittent に
# WRONG_VERSION_NUMBER で落ちる (squid が「接続先実 IP」と「SNI 再解決 IP 集合」を
# 照合し、短 TTL で IP がズレるため)。明示 proxy なら CONNECT <hostname>:443 を送るので
# 宛先 IP 照合自体が起きず安定する。squid のあるホスト(dev-server)限定の安価なガード:
# squid.conf が読めるときだけ export する (TCP 接続チェックはしない = 起動を遅くしない)。
if [[ -r /etc/squid/squid.conf ]]; then
  # export HTTP_PROXY="http://127.0.0.1:3128"  http_proxy="http://127.0.0.1:3128"
  # export HTTPS_PROXY="http://127.0.0.1:3128" https_proxy="http://127.0.0.1:3128"
  # # 直結が必須なものだけ最小限。ここに入れた宛先は proxy を通らず intercept 側へ落ちる。
  # export NO_PROXY="169.254.169.254,localhost,127.0.0.1,::1"
  # export no_proxy="$NO_PROXY"
fi

# --- AWS env (credential 本体は持たせない: wrapper/SendEnv/dotenvx 等の注入に任せる) ---
# AWS_PROFILE が空文字だと (例: laptop の SendEnv 経由で空のまま転送される)、未設定とは
# 違って profile 解決に失敗する。env 変数 credential 運用で profile は不要なので空なら unset。
[[ -z "${AWS_PROFILE:-}" ]] && unset AWS_PROFILE
# 既定 region を持たせ `--region` を毎回付けずに済ませる (未設定/空のときだけ・転送値は尊重)。
# CLI は AWS_DEFAULT_REGION、一部 SDK は AWS_REGION を見るため両方に入れる。
: "${AWS_DEFAULT_REGION:=ap-northeast-1}"
: "${AWS_REGION:=ap-northeast-1}"
export AWS_DEFAULT_REGION AWS_REGION

# zsh-syntax-highlighting は必ず最後に source する。
[ -r /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh ] && \
  source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
